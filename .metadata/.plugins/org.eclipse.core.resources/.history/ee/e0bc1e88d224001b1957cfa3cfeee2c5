package com.exam.repository;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

import com.exam.model.User;

@Repository
public interface UserRepository extends JpaRepository<User, Integer>
{

	User findOne(String userEmail);
	//@Query("select userEmail,userPassword from User")
	//public List<User> findByEmailAndPassword(String userEmail,String userPassword);
	//public User findByEmailAndPassword(String userEmail, String userPassword);
	
}
